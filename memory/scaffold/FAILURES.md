# FAILURES.md — journal des échecs

<!-- Append-only. Une ligne par échec : `- [horodatage] description`.
     Alimenté par Jarvis lui-même (omarchy-jarvis note) et par la pipeline
     (transcript vide, cerveau injoignable, barge-in). Consolidé puis vidé
     par `omarchy-jarvis dream`. -->

