# ABORTS.md — journal des annulations

<!-- Append-only. Une ligne par annulation : `- [horodatage] annulé pendant
     <étape> après <n> s (<qui>)`. Écrit par la pipeline (bin/jarvis,
     note_abort), jamais par Jarvis lui-même.

     Une annulation n'est PAS un échec. C'est l'utilisateur qui reprend la
     main, et c'est précisément pourquoi ces lignes ne sont plus dans
     FAILURES.md : une seule entrée là-bas suffit à programmer un rêve dont
     le métier est de réécrire LEARNED.md, donc la personnalité. Appuyer sur
     une touche fabriquait des changements de caractère permanents.

     Les rêves n'y cherchent qu'un MOTIF répété (voir DREAM_PROMPT.md, étape
     4), jamais un incident isolé. Rien n'y est noté sous deux secondes :
     ce n'est pas un motif, c'est un doigt qui a ripé. -->

